PORT=3000
SESSION_SECRET=change-me-to-a-random-64-char-string
ENCRYPTION_KEY=change-me-to-a-64-hex-char-string
WG_INTERFACE=wg0
WG_SUBNET=10.0.0.0/24
WG_SERVER_IP=10.0.0.1
WG_ENDPOINT=your.server.public.ip:51820
WG_DNS=1.1.1.1
WG_ALLOWED_IPS=0.0.0.0/0
DB_PATH=./data/wg-portal.db

# Docker-specific
WG_PORT=51820
HTTP_PORT=80
COOKIE_SECURE=false
PAM_SERVICE=wg-portal
TAG=latest
# WG_NAT_IFACE=ens19        # NAT output interface (auto-detect if empty)

# LDAP (optional - leave empty to use PAM only)
# LDAP_URL=ldap://your-ldap-server:389
# LDAP_BIND_DN=cn=admin,dc=example,dc=com
# LDAP_BIND_PASSWORD=admin-password
# LDAP_BASE_DN=dc=example,dc=com
# LDAP_USER_FILTER=(uid={username})
# LDAP_ADMIN_GROUP=admin
# LDAP_TLS_REJECT_UNAUTHORIZED=false   # Set to false for self-signed certs
